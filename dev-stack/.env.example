# Local development stack — copy to .env and adjust as needed
FRONTEND_PORT=3000
BACKEND_PORT=8000
POSTGRES_HOST_PORT=5432
POSTGRES_TEST_DB=jobs_test

# Change POSTGRES_HOST_PORT if port 5432 is already occupied on the host.
