# Local development stack — copy to .env and adjust as needed
FRONTEND_PORT=3000
BACKEND_PORT=8000

# Optional: expose Postgres on the host for external DB tools (leave unset to keep internal-only)
# POSTGRES_HOST_PORT=5432
